SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# The only email allowed into this panel -- e.g. harin.25bcs10680@sst.scaler.com
# There's no password: visiting the site sends a magic link to exactly this
# address via Supabase Auth, and nothing else is ever emailed anywhere.
ADMIN_EMAIL=

# This app's own deployed URL (or http://localhost:3000 locally), used as
# the magic-link redirect target.
ADMIN_SITE_URL=http://localhost:3000
